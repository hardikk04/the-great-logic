import React from "react";
import StickyCards from "./StickyCards";

const StickyWrapper = () => {
  return (
    <section className="relative flex justify-center pt-30 md:px-6">
      <StickyCards />
    </section>
  );
};

export default StickyWrapper;
